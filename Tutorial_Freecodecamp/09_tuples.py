# tuples
coordinates = (4, 5)
# tuples are immutable
print(coordinates)
print(coordinates[0])  # accessing values in tuples
#gives an error as tuples are immutable
#coordinates[1] = 10


# we can create a list of tuples
coordinates_list = [(1,1),(4,2)]

